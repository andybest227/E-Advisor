<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EAdvisor" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:popupBackground">#FFFFFF</item> <!-- White dropdown background -->
        <item name="android:textColor">#000000</item>       <!-- Black text -->
    </style>

    <style name="CustomChipGroup" parent="Widget.MaterialComponents.ChipGroup">
        <item name="android:background">#FFFFFF</item> <!-- White dropdown background -->
        <item name="android:textColor">#000000</item>       <!-- Black text -->
    </style>

    <style name="Theme.EAdvisor.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:statusBarColor">#FFFFFF</item> <!-- White status bar background -->
        <item name="android:windowLightStatusBar">true</item> <!-- Black icons in status bar -->

        <!-- Fix EditText color issues -->
        <item name="colorPrimary">@color/dark_blue</item> <!-- Adjust to your brand color -->
        <item name="colorOnSurface">#000000</item>
        <item name="android:textColor">#000000</item>
        <item name="android:editTextBackground">?android:attr/editTextBackground</item>
        <item name="spinnerStyle">@style/CustomSpinner</item>
        <item name="chipGroupStyle">@style/CustomChipGroup</item>
    </style>

    <style name="Theme.EAdvisor.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.EAdvisor.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EAdvisor.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>